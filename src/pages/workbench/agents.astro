---
import Workbench from '../../layouts/Workbench.astro';
---
<Workbench title="Agents" spec="/docs/v1-plan/workbench/">
  <p>Create agents, edit slugs / display names, set defaults, create main channel.</p>
  <p style="color:var(--dim);">Owner/admin write; member read. Backed by <code>agents</code> + <code>agent_channels</code>.</p>
</Workbench>
